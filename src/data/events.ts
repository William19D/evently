import { Event } from "@/types/event";

// Datos de ejemplo eliminados - ahora se obtienen desde la base de datos
export const sampleEvents: Event[] = [];

export const getEventById = (id: string): Event | undefined => {
  // Esta función ya no devuelve datos hardcodeados
  // Los eventos deben obtenerse desde la API/base de datos
  return undefined;
};
